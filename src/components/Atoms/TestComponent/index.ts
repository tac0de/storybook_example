/**
 * TestComponent 인덱스 파일
 *
 * 이 파일은 컴포넌트를 외부에서 import할 때 사용됩니다.
 * barrel export 패턴을 사용하여 깔끔한 import 구문을 제공합니다.
 */

export { TestComponent } from './TestComponent';
export type { TestComponentProps } from './TestComponent';
